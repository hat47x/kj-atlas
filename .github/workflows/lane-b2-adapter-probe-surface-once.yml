name: Lane B2 adapter probe-surface contract once

on:
  push:
    branches:
      - docs/lane-b2-adapter-probe-surface-20260907

permissions:
  contents: write

jobs:
  patch-verify-retire:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Apply adapter probe-surface patch
        run: python .github/lane-b2-adapter-probe-surface-patch.py
      - name: Verify focused adapter probe-surface contract
        run: python -m unittest 01_Plans.tests.test_runtime_adapter_probe_surface_contract
      - name: Verify planning contracts
        run: python -m unittest discover -s 01_Plans/tests -p 'test_*.py'
      - name: Verify documentation contracts
        run: python 01_Plans/docs_check.py
      - name: Verify narrow diff
        shell: bash
        run: |
          git diff --check
          python - <<'PY'
          import subprocess
          expected = {
              '01_Plans/tests/test_runtime_adapter_probe_surface_contract.py',
              '02_Architecture/runtime_parameter_registry.md',
          }
          tracked = set(filter(None, subprocess.check_output(['git', 'diff', '--name-only'], text=True).splitlines()))
          untracked = set(filter(None, subprocess.check_output(['git', 'ls-files', '--others', '--exclude-standard'], text=True).splitlines()))
          actual = tracked | untracked
          if actual != expected:
              raise SystemExit(f'narrow diff mismatch: expected={sorted(expected)} actual={sorted(actual)}')
          PY
      - name: Commit patch and retire one-shot helpers
        shell: bash
        run: |
          rm .github/lane-b2-adapter-probe-surface-patch.py
          rm .github/workflows/lane-b2-adapter-probe-surface-once.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'docs(config): adapter probeを実観測面へ同期する'
          git push
