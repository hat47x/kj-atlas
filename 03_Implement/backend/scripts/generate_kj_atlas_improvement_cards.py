#!/usr/bin/env python3
"""Generate the 200-card round-1 canvas for kj-atlas self-improvement dogfooding.

Each card is a single, concrete observation about kj-atlas (its AI operations,
role/authorization model, hierarchy, W-type rounds, operations, value realization,
and the dogfooding harness itself). Observations are drawn from the project's own
issues (DOGFOOD-11..31), ADRs, and the three-element design method — the real
improvement opportunities surfaced by dogfooding, not placeholders.

Card texts are predicate sentences (述語文), one idea per card, with a (領域)
category suffix used by the deterministic mock's theme-based grouping.

Usage: python3 generate_kj_atlas_improvement_cards.py > doc.json
"""
from __future__ import annotations

import json

# (id, text) — text is a 述語文 ending in a verb, one idea, with a （領域）suffix.
CARDS: list[tuple[str, str]] = [
    # --- AI検証深度 (DOGFOOD-11..30) ---
    ("ai-01", "矛盾検出に決定的な正パスがなく、常にfalseで回帰を通す（AI検証深度）"),
    ("ai-02", "A/B照合の正パスが島ID「i1」に固定され、他島で422になる（AI検証深度）"),
    ("ai-03", "島要約の接地が3カードで打ち切られ、4枚以上の島を検証できない（AI検証深度）"),
    ("ai-04", "A/B照合にb_missing_in_a方向の正パスがなく、双方向を固定できない（AI検証深度）"),
    ("ai-05", "島間関係の接地が常に空で、関係の根拠を検証できない（AI検証深度）"),
    ("ai-06", "文面整えが元の意味を保持することを検証できない（AI検証深度）"),
    ("ai-07", "反対視点が対象カードの主張に応答せず、接地していない（AI検証深度）"),
    ("ai-08", "タイトル提案が文書のテーマに接地しない（AI検証深度）"),
    ("ai-09", "マージ提案に決定的な正パスがなく、統合候補を検証できない（AI検証深度）"),
    ("ai-10", "AI束ねが位置ベースで、テーマ類似性を検証できない（AI検証深度）"),
    ("ai-11", "ナラティブ本文が読み順の島を参照しない（AI検証深度）"),
    ("ai-12", "CE4提案の内容・接地がE2Eで未検証である（AI検証深度）"),
    ("ai-13", "配置提案の全カード保持を検証していない（AI検証深度）"),
    ("ai-14", "A/B照合が最初の島だけを報告し、複数島の取りこぼしを検出しない（AI検証深度）"),
    ("ai-15", "タイトル提案が最初の島ラベルだけを参照する（AI検証深度）"),
    ("ai-16", "矛盾検出の説明文がカード対を参照しない（AI検証深度）"),
    ("ai-17", "関係要約の本文が島ペアを参照しない（AI検証深度）"),
    ("ai-18", "島要約の表札が島のテーマを参照しない（AI検証深度）"),
    ("ai-19", "配置提案の島・読み順の保持を検証していない（AI検証深度）"),
    ("ai-20", "島要約プロンプトがjson.dumpsでエスケープされ、テーマ抽出にデコードが要る（AI検証深度）"),

    # --- 実規模対応 (DOGFOOD-31) ---
    ("sc-01", "card-groupsの入力上限が100枚で、200枚の束ねを遮る（実規模）"),
    ("sc-02", "島要約の接地が10件上限で、大きな島の表札が書けない（実規模）"),
    ("sc-03", "第1ラウンドで200枚のカードを作る実践と、AI操作上限が乖離する（実規模）"),
    ("sc-04", "kj_technique.mdは数百枚を正常とするが、実装は小規模前提である（実規模）"),
    ("sc-05", "大量カードの束ねがカードを欠落させないことを検証していない（実規模）"),
    ("sc-06", "200枚の文書を叙述・A/B照合できるか実規模で検証していない（実規模）"),
    ("sc-07", "大量カードの読戻しが全件保持することを検証していない（実規模）"),
    ("sc-08", "接地の全メンバー化(DOGFOOD-13)と10件上限が逆方向に衝突する（実規模）"),
    ("sc-09", "実物大の島（数十〜百枚）で表札が接地できない（実規模）"),
    ("sc-10", "100枚/10件の上限はドキュメント境界(1万枚)と整合していない（実規模）"),
    ("sc-11", "束ねの上限緩和が文書境界(max_document_cards)と整合する必要がある（実規模）"),
    ("sc-12", "大量カード投入時のパフォーマンスを検証していない（実規模）"),
    ("sc-13", "200枚のカードのキャンバス配置を検証していない（実規模）"),
    ("sc-14", "実規模のKJ実践で束ね→島統合の一気通貫が未検証である（実規模）"),
    ("sc-15", "大量カードのAI操作が課金・レイテンシ面で検証されていない（実規模）"),
    ("sc-16", "接地の品質ガード（10件）と全メンバー原則の折り合いが未整理である（実規模）"),
    ("sc-17", "大規模キャンバスでのSafeModeの挙動を検証していない（実規模）"),
    ("sc-18", "200枚のカードのカテゴリ別束ねの網羅性を検証していない（実規模）"),
    ("sc-19", "実規模の島統合で表札の代弁性が保たれるか検証していない（実規模）"),
    ("sc-20", "大量カードのA型図解→B型文章化が実規模で未検証である（実規模）"),

    # --- 階層・図解 ---
    ("hi-01", "階層島(parentIslandId)の往復保持がE2Eで未固定である（階層・図解）"),
    ("hi-02", "親島・子島の関係を島要約が保持することを検証していない（階層・図解）"),
    ("hi-03", "階層化された島の関係線をA型図解として固定していない（階層・図解）"),
    ("hi-04", "関係線の5種別（因果/対立等）を踏まえた図解が未検証である（階層・図解）"),
    ("hi-05", "階層島のナラティブが読み順を叙述することを検証していない（階層・図解）"),
    ("hi-06", "A/B照合が複数階層の島の取りこぼしを検出することを検証していない（階層・図解）"),
    ("hi-07", "parentIslandIdの循環参照フォールバックをE2Eで検証していない（階層・図解）"),
    ("hi-08", "階層島のimport正規化（undefinedフォールバック）を検証していない（階層・図解）"),
    ("hi-09", "上位島への統合（抽象度の上昇）をE2Eで検証していない（階層・図解）"),
    ("hi-10", "階層化の際のカード帰属の保持を検証していない（階層・図解）"),
    ("hi-11", "多層図解（全体インデックス＋細部）が未対応である（階層・図解）"),
    ("hi-12", "島の親子関係と関係線の相互作用が未検証である（階層・図解）"),
    ("hi-13", "階層島の島間関係要約が島ペアを参照することを検証していない（階層・図解）"),
    ("hi-14", "A型図解からB型文章化への一気通貫が未検証である（階層・図解）"),
    ("hi-15", "階層化した島の読戻しで構造が保たれるか検証していない（階層・図解）"),
    ("hi-16", "表札の代弁性が階層（親島・子島）で保たれるか検証していない（階層・図解）"),
    ("hi-17", "階層島の配置提案が島・読み順を保持するか検証していない（階層・図解）"),
    ("hi-18", "上位島の接地（子島の代表カード）が未定義である（階層・図解）"),
    ("hi-19", "階層化とW型ラウンドの成果の関係が未整理である（階層・図解）"),
    ("hi-20", "多層図解の画面表示（可読性）が未検証である（階層・図解）"),

    # --- W型・ラウンド ---
    ("wr-01", "W型6ラウンド（R1問題提起〜R6手順計画）がE2Eで未検証である（W型・ラウンド）"),
    ("wr-02", "ラウンド段階（R1〜R6）と反復番号の分離を検証していない（W型・ラウンド）"),
    ("wr-03", "ラウンド成果（RoundSnapshot）が読取専用で保存されることを検証していない（W型・ラウンド）"),
    ("wr-04", "ラウンド間の引き継ぎ（出典・根拠のtraceability）が未検証である（W型・ラウンド）"),
    ("wr-05", "同じ段階を複数回反復（RoundIteration）することを検証していない（W型・ラウンド）"),
    ("wr-06", "後続ラウンドから前の問いへ戻る（フィードバック）を検証していない（W型・ラウンド）"),
    ("wr-07", "中断後の同じラウンド成果からの再開を検証していない（W型・ラウンド）"),
    ("wr-08", "inquiry-bundleの楽観的並行制御（CAS）を6ラウンドで検証していない（W型・ラウンド）"),
    ("wr-09", "ラウンド名だけ付けて省略過程を実施済みと見なすことを防ぐ検証がない（W型・ラウンド）"),
    ("wr-10", "6ラウンドの各段階で狭義KJ法一ラウンド全過程を実施できるか未検証である（W型・ラウンド）"),
    ("wr-11", "ラウンド間でカード本文・配置・島・関係・文章化を再現できるか未検証である（W型・ラウンド）"),
    ("wr-12", "ラウンド成果が編集対象の複製でなく読取専用履歴であることを検証していない（W型・ラウンド）"),
    ("wr-13", "探究（InquiryJourneyV1）とラウンド記録（RoundRecordV1）の関係が未検証である（W型・ラウンド）"),
    ("wr-14", "W型の累積KJ法が通常一ラウンド利用を非後退させることを検証していない（W型・ラウンド）"),
    ("wr-15", "ラウンドの分岐（前段階分岐）を検証していない（W型・ラウンド）"),
    ("wr-16", "6ラウンドの叙述（generate-narrative）が階層・読み順を叙述するか未検証である（W型・ラウンド）"),
    ("wr-17", "ラウンド成果の自己完結bundleを検証していない（W型・ラウンド）"),
    ("wr-18", "provider=none時のW型ラウンド保存が成立するか検証していない（W型・ラウンド）"),
    ("wr-19", "W型ラウンドとSafeModeの相互作用が未検証である（W型・ラウンド）"),
    ("wr-20", "ラウンド間の問いの変化（テーマ）を追跡する仕組みが未検証である（W型・ラウンド）"),

    # --- ロール・認可 ---
    ("ro-01", "システム管理者が業務面の文書へ到達できないことをE2Eで検証していない（ロール・認可）"),
    ("ro-02", "業務管理者がテナント境界を越えて他テナント文書へ到達できないことを検証していない（ロール・認可）"),
    ("ro-03", "閲覧専用ユーザがwriteできないことをE2Eで検証していない（ロール・認可）"),
    ("ro-04", "閲覧専用ユーザがAI提案を決定できないことを検証していない（ロール・認可）"),
    ("ro-05", "一般ユーザが管理面へ到達できないことを検証していない（ロール・認可）"),
    ("ro-06", "業務面キーで管理面へ到達すると401になることをE2Eで検証していない（ロール・認可）"),
    ("ro-07", "roles/groupsがIdP由来のtransient権限であることを検証していない（ロール・認可）"),
    ("ro-08", "effectiveCapabilitiesがbackendで再認可されることを検証していない（ロール・認可）"),
    ("ro-09", "tenant横断の本文検索・恒久super-readerを標準機能にしないことを検証していない（ロール・認可）"),
    ("ro-10", "4ロールで1つのKJ実践を貫く境界固定が未検証である（ロール・認可）"),
    ("ro-11", "break-glass（時間制限・承認・監査）が別ADRで扱われることを検証していない（ロール・認可）"),
    ("ro-12", "管理面の監査証跡（主体・時刻・対象）がread-only参照できることを検証していない（ロール・認可）"),
    ("ro-13", "文書の共有設定・visibilityがラベル用途に限定されることを検証していない（ロール・認可）"),
    ("ro-14", "read-only MCP経由の投影読み取りが監査送出されることを検証していない（ロール・認可）"),
    ("ro-15", "管理credentialが業務キーを転用しないことを検証していない（ロール・認可）"),
    ("ro-16", "tenantはroleの一種でなく構造境界であることを検証していない（ロール・認可）"),
    ("ro-17", "管理者の秘密値がshell履歴・引数に露出しないことを検証していない（ロール・認可）"),
    ("ro-18", "一般ユーザのKJ実践（CRUD+AI採否）が管理面と分離されることを検証していない（ロール・認可）"),
    ("ro-19", "閲覧専用ユーザが確定点・保留点・根拠を読み解けるか検証していない（ロール・認可）"),
    ("ro-20", "モデルallowlistの設定がテナント境界内で運用されることを検証していない（ロール・認可）"),

    # --- コラボ・可読性 ---
    ("co-01", "AI提案がauto-applyされない（proposal-only）ことをE2Eで検証していない（コラボ・可読性）"),
    ("co-02", "AIがhuman_reviewedを自動付与しないことを検証していない（コラボ・可読性）"),
    ("co-03", "AI由来と人間由来を明確に区別することを検証していない（コラボ・可読性）"),
    ("co-04", "表札が島を代弁する述語文（名詞止めでない）であることを検証していない（コラボ・可読性）"),
    ("co-05", "ナラティブが図解に接地（読み順の島を参照）することを検証していない（コラボ・可読性）"),
    ("co-06", "レビューパックが確定点・保留点・根拠を明示することを検証していない（コラボ・可読性）"),
    ("co-07", "AI自動生成の解答と人間×AI協働の洞察の質を対比する検証がない（コラボ・可読性）"),
    ("co-08", "中間処理（分類/要約）と最終判断（採否）の二層化を検証していない（コラボ・可読性）"),
    ("co-09", "最終判断層でも自動確定を禁止することを検証していない（コラボ・可読性）"),
    ("co-10", "人間のレビュー状態が明示されることを検証していない（コラボ・可読性）"),
    ("co-11", "保留・違和感を失敗として扱わず保存できることを検証していない（コラボ・可読性）"),
    ("co-12", "AI提案の根拠（接地）を人間が確認して採否できることを検証していない（コラボ・可読性）"),
    ("co-13", "画面表示が人間に読み解けるか（可読性）を定性的に評価していない（コラボ・可読性）"),
    ("co-14", "表札の戻し検査（一枚でも違うと返る）を検証していない（コラボ・可読性）"),
    ("co-15", "反対視点・根拠不足を提案のみ（人間が判断）で表示することを検証していない（コラボ・可読性）"),
    ("co-16", "ナラティブが事実の主張でなく解釈の下書きであることを検証していない（コラボ・可読性）"),
    ("co-17", "AI提案の採否（adopt/hold/reject）が人間に委ねられることを検証していない（コラボ・可読性）"),
    ("co-18", "未レビュー文がSafeModeでAIへ送られない（422）ことを検証していない（コラボ・可読性）"),
    ("co-19", "読者が確定点と保留点を区別できることを検証していない（コラボ・可読性）"),
    ("co-20", "表札が別の島に載せても成立しない（転置検査）ことを検証していない（コラボ・可読性）"),

    # --- 運用 ---
    ("op-01", "正式CLIがモデル・tenant・IdP・監査照会を扱えない（運用）"),
    ("op-02", "管理コンソールが未整備でcurl/自作script依存が続く（運用）"),
    ("op-03", "監査照会がcurl依存で、変更前後差分を見落としやすい（運用）"),
    ("op-04", "秘密値がshell履歴・process引数に露出する経路が残る（運用）"),
    ("op-05", "管理者のwriteに変更previewと対話確認が必須でない（運用）"),
    ("op-06", "CLIがbusiness-plane keyを環境変数だけで送るよう統一されていない（運用）"),
    ("op-07", "MCP検証がCE4提案の完全ライフサイクル（proposed→decided）を検証していない（運用）"),
    ("op-08", "admin ops E2EがCLIモデルレジストリ運用まで拡張されていない（運用）"),
    ("op-09", "監査のdenied操作がresult=deniedとして記録されることを検証していない（運用）"),
    ("op-10", "provider-statusのcallCountsがUIで可視化されることを検証していない（運用）"),
    ("op-11", "モデルallowlistの設定が管理APIで一貫して検証されていない（運用）"),
    ("op-12", "空DBから認証成立までの通し検証が未実施である（運用）"),
    ("op-13", "管理面の起動時fail-fast（認証未設定で拒否）を検証していない（運用）"),
    ("op-14", "admin audit eventsの照会（allowlist・composite cursor）を検証していない（運用）"),
    ("op-15", "CLIのCE4監査操作がキー有効backendへ接続できない問題が残る（運用）"),
    ("op-16", "利用者SPAへの管理導線が0件で、管理操作が発見されない（運用）"),
    ("op-17", "MCP read→CE-4監査→HTTPシンク到達を自己完結E2Eで検証していない（運用）"),
    ("op-18", "LLM呼び出し量の監視（コスト）が運用面で参照できない（運用）"),
    ("op-19", "診断バンドル・エラー回復が未整備である（運用）"),
    ("op-20", "管理面と利用者SPAが別origin・別配備・短時間sessionで分離されていない（運用）"),

    # --- 価値実現 ---
    ("va-01", "外在化（V1）としてメモや違和感を置けることを検証していない（価値実現）"),
    ("va-02", "構造化（V2）としてまとまり・関係・未整理を同時に扱えることを検証していない（価値実現）"),
    ("va-03", "レビュー（V3）としてAI候補を人間が採否できることを検証していない（価値実現）"),
    ("va-04", "共有と学習（V4）として読者が確定点・保留点・根拠を理解できることを検証していない（価値実現）"),
    ("va-05", "SafeModeが既定ONで未レビュー本文を混入させないことを検証していない（価値実現）"),
    ("va-06", "保留・違和感・未整理を失敗として扱わないことを検証していない（価値実現）"),
    ("va-07", "意味を急いで確定しない（考え途中の状態保存）を検証していない（価値実現）"),
    ("va-08", "人間の判断を優先する（proposal-only）を検証していない（価値実現）"),
    ("va-09", "思考を雑にしない（複雑性予算CB-1..4）を検証していない（価値実現）"),
    ("va-10", "価値を裏切らない（不変条件の保護CVI-1..7）を検証していない（価値実現）"),
    ("va-11", "生成AI経路を混同しない（Lane A/B/C/Dの分離）を検証していない（価値実現）"),
    ("va-12", "ラウンド間で思考を深める（W型累積）を検証していない（価値実現）"),
    ("va-13", "カードメタデータを混同しない（状態・出典・起票者）を検証していない（価値実現）"),
    ("va-14", "初回価値実感（3枚以上のカードでまとまり・関係・保留）を検証していない（価値実現）"),
    ("va-15", "根拠・主張・反対意見の追跡を検証していない（価値実現）"),
    ("va-16", "成果物化（narrative grounding・review pack）を検証していない（価値実現）"),
    ("va-17", "共有前に未レビュー情報・保留点・根拠が明示されることを検証していない（価値実現）"),
    ("va-18", "価値実現ループ（V1〜V4）が閉じていることを検証していない（価値実現）"),
    ("va-19", "SafeMode/readOnly/visibilityの優先順位が崩れないことを検証していない（価値実現）"),
    ("va-20", "検証観点が上流の価値判断（value_traceability）に接続されることを検証していない（価値実現）"),

    # --- 三要素・設計 ---
    ("de-01", "設計判断が三要素（業務/データ/機能）牽制を通ることを検証していない（三要素・設計）"),
    ("de-02", "三者が揃わない設計判断を着工しないことを検証していない（三要素・設計）"),
    ("de-03", "業務設計（誰が・何のために・何をしないか）の境界が未検証である（三要素・設計）"),
    ("de-04", "データ設計（何が保存・表示され、何が境界を越えるか）が未検証である（三要素・設計）"),
    ("de-05", "機能設計（誰が・どのAPIで・どの状態遷移で操作するか）が未検証である（三要素・設計）"),
    ("de-06", "API契約の継ぎ目ドリフト（単一表現）を検証していない（三要素・設計）"),
    ("de-07", "document契約のrebaseline（ADR-0058）を検証していない（三要素・設計）"),
    ("de-08", "設計判断の記録形式（ADR/issue）が揺れないことを検証していない（三要素・設計）"),
    ("de-09", "操作別モデルレベル（推論深度）の選択を検証していない（三要素・設計）"),
    ("de-10", "自律性レベル（L1〜L4）の昇格条件を定量で判定することを検証していない（三要素・設計）"),
    ("de-11", "SafeMode・共有・export・AI提案の安全不変条件を緩和しないことを検証していない（三要素・設計）"),
    ("de-12", "変更リスクに応じたテスト実行（docs-check/unit/integration/e2e）を検証していない（三要素・設計）"),
    ("de-13", "新規設計判断を含むissueが三要素整合を宣言することを検証していない（三要素・設計）"),
    ("de-14", "契約チェックポイント（schema/API/policyとの交差）を検証していない（三要素・設計）"),
    ("de-15", "停止基準（要件未確定・競合発生時にProceedしない）を検証していない（三要素・設計）"),
    ("de-16", "AIレーン宣言（Lane A/B/C/D）がissueで宣言されることを検証していない（三要素・設計）"),
    ("de-17", "設計判断がADRへ記録されるべきか（長期的・横断的・破壊的）を判定していない（三要素・設計）"),
    ("de-18", "価値トレーサビリティ（価値判断→設計→検証観点）を検証していない（三要素・設計）"),
    ("de-19", "認知負荷の予算（初期表示の純増・保留距離）を検証していない（三要素・設計）"),
    ("de-20", "三要素整合チェックリスト（18項目）の適用実例が不足している（三要素・設計）"),

    # --- ドッグフーディング運用 ---
    ("dg-01", "検証ハーネスが決定性モックLLMで課金なしに回ることを検証していない（ドッグフーディング）"),
    ("dg-02", "業務フローE2Eのカバレッジ（業態・操作・注意事項）を集約していない（ドッグフーディング）"),
    ("dg-03", "構造照合（verify_dogfood_records.sh）が数値・算術・課題状態を照合することを検証していない（ドッグフーディング）"),
    ("dg-04", "ドッグフーディング観察由来の課題を三要素分析付きで起票していない（ドッグフーディング）"),
    ("dg-05", "検証は正常系も異常系もassertする（DOGFOOD-06）ことを検証していない（ドッグフーディング）"),
    ("dg-06", "モックの検証深度（正パス・接地・テキスト参照）を段階的に向上させることを検証していない（ドッグフーディング）"),
    ("dg-07", "並行イテレーションのレース（DOGFOOD-10）を回避することを検証していない（ドッグフーディング）"),
    ("dg-08", "ループ運用のsingle-flight化・snapshot化を検証していない（ドッグフーディング）"),
    ("dg-09", "E2Eが実行中に入力が変わらない前提をassertすることを検証していない（ドッグフーディング）"),
    ("dg-10", "検証経路（Web/API/MCP/CLI）を新規追加・拡張する規約を検証していない（ドッグフーディング）"),
    ("dg-11", "モックが実LLM品質と独立である（決定性表現）ことを検証していない（ドッグフーディング）"),
    ("dg-12", "定性的な洞察の質評価（AI-EVAL-01）を人間評価と併用することを検証していない（ドッグフーディング）"),
    ("dg-13", "検証ハーネスの盲点をイテレーション毎に是正することを検証していない（ドッグフーディング）"),
    ("dg-14", "カバレッジ集約（業態・操作カバー）が自動更新されることを検証していない（ドッグフーディング）"),
    ("dg-15", "docs_checkが有効化ruleだけをblocking実行することを検証していない（ドッグフーディング）"),
    ("dg-16", "active issue memoの検証（validate_active_issue_memos）を検証していない（ドッグフーディング）"),
    ("dg-17", "タスク候補の絞り込み（triage_actionable_plans）を検証していない（ドッグフーディング）"),
    ("dg-18", "ドッグフーディングの成果を一か所集約（マニフェスト）することを検証していない（ドッグフーディング）"),
    ("dg-19", "別の生成AIが再現実行できる検証構造を提供することを検証していない（ドッグフーディング）"),
    ("dg-20", "高度ドッグフーディング（200枚・階層・W型・ロール）をE2E題材化していない（ドッグフーディング）"),
]


def build_document() -> dict:
    cards = [
        {"id": cid, "text": text, "x": (i % 10) * 28, "y": (i // 10) * 16, "textReviewed": True}
        for i, (cid, text) in enumerate(CARDS)
    ]
    return {
        "version": 1,
        "id": "biz-flow-200cards",
        "title": "kj-atlas改善のカード200枚（第1ラウンド）",
        "createdAt": "2026-08-17T00:00:00Z",
        "updatedAt": "2026-08-17T00:00:00Z",
        "transform": {"panX": 0, "panY": 0, "zoom": 1},
        "cards": cards,
        "edges": [],
        "islands": [{"id": "all-i", "cardIds": [c["id"] for c in cards]}],
        "readingOrder": ["all-i"],
    }


if __name__ == "__main__":
    print(json.dumps(build_document(), ensure_ascii=False))
