# Governance / ガバナンス

## プロジェクトの目的

`kj-atlas` は、KJ法インスパイアの図解・意味探索を
**human-in-the-loop** で支援するためのプロジェクトです。

- 正解の自動生成よりも、保留・違和感・再配置可能性を重視します。
- 設計判断は `00_Prompt` / `01_Plans` / `02_Architecture` を優先します。

## メンテナモデル / Maintainer Model

- 当面は **single-maintainer** 運用です。
- レビュー・応答・リリースは best effort で実施します。
- 長期間応答できない場合、Issue/PR 上で引き継ぎ候補を募ることがあります。

## 意思決定 / Decision Making

- 重要な決定（仕様変更、互換性破壊、依存方針変更など）は
  Issue または PR で記録します。
- 原則として公開スレッドで合意形成し、記録を残します。
- 合意が難しい場合は、メンテナが最終判断を行います。

## リリース方針 / Release Policy

- リリースは必要に応じて実施します（time-based ではありません）。
- タグは `vX.Y.Z` 形式を推奨します（厳密なSemVer運用は将来拡張）。
- 破壊的変更がある場合は、CHANGELOG またはリリースノートで明示します。
