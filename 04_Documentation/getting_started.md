# 最初の意味ある配置を作る

対象読者: kj-atlas を初めて起動し、基本的な価値を安全に体験したい利用者・評価担当者。

目的: AIを使わず、秘密情報を含まない標準サンプルから、3件の観察を読み、近い2件を一つのまとまりにし、残る1件を無理にまとめず`未決`として残します。そのうえで、保存・再読込・共有前確認まで進みます。

範囲外: AI提案、自動分類、実データの取り込み、ファイルの書き出し、他者への共有、クラウド同期、全機能の動作確認。

完了状態: 「なぜ2件をまとめたのか」「なぜ1件はまだ同じまとまりに入れなかったのか」「外部共有を実行していないこと」を、自分の言葉で説明できます。

所要時間の目安は10分です。機能全体を検査したい場合は、この教程ではなく[受け入れ確認](acceptance_check.md)を使ってください。

## 始める前の安全確認

1. [導入手順](installation.md)に従ってアプリを起動し、ブラウザで開きます。
2. LLM provider は既定の `KJ_ATLAS_LLM_PROVIDER=none` のままにします。
3. 「作業を開始」パネルの安全状態が `セーフモード: ON` であることを確認します。OFFならここで止め、[設定ガイド](configuration.md)と[セキュリティ](security.md)を確認してください。
4. この教程では標準サンプルだけを使います。氏名、顧客情報、API key、token、passwordなどを入力しないでください。

![SafeModeを確認できる作業開始パネル](assets/screenshots/start-document-entry.png)

## この教程で使う題材

`サンプルを開く`と、次の3件の観察が表示されます。

1. 「インタビュー後、メモがチャットの奥に流れて、見つけ直すのに時間がかかった。」
2. 「結論は残っているのに、どの発言を根拠にしたのか後から分からなくなった。」
3. 「一人だけ違う意見だったので、そのまま議事録から落ちた。」

この3件は操作確認のために作った架空の観察です。実際の利用者調査や第三者評価から得た証拠ではありません。

この教程では、1と2を「後から根拠へ戻りにくくなる」という近さから一つの島にします。一方、3も情報が失われる話ではありますが、「少数意見が記録から消える」という別の含みがあります。そこで、最初から同じまとまりだと決めず、`未決`として残します。

ここで大切なのは、この分け方を唯一の正解として覚えることではありません。**近いと感じた理由を確かめ、まだ同じだと言い切れないものを無理に閉じないこと**を体験してください。

## 9つの手順

| 手順 | 目的と操作 | 期待する状態 | 取消・安全な停止 |
| --- | --- | --- | --- |
| 1 | 「作業を開始」で `サンプルを開く` を選びます。 | 開始パネルが閉じ、キャンバスが表示されます。 | 開けない場合は入力を続けず、[診断と障害調査](diagnostics.md)へ進みます。 |
| 2 | 上記3件の観察カードが見えることを確認し、まず内容を読みます。 | 秘密情報を含まない固定題材が3件あり、まだ島にはまとめられていません。 | 文言や件数が違う場合は、実データを追加せず教程を止めます。 |
| 3 | 「インタビュー後、メモがチャットの奥に流れて、見つけ直すのに時間がかかった。」を選び、Shiftを押しながら「結論は残っているのに、どの発言を根拠にしたのか後から分からなくなった。」を選びます。 | 2件が選択状態になり、ヘッダーの `島を作成` が使えます。 | Escまたはキャンバスの空白を選び、選択を解除できます。 |
| 4 | 2件に共通する「後から根拠へ戻りにくい」という近さを確かめてから、ヘッダーの `島を作成` を選びます。 | 「選択したカード 2 件から島を作成しました」と表示され、2カードを含むまとまりができます。 | 共通点に納得できない場合は作成しなくてかまいません。作成後はCtrl+Z（macOSはCmd+Z）で元に戻せます。戻した場合はCtrl+YまたはCmd+Shift+Zでやり直します。 |
| 5 | Escで島の選択を解除します。「一人だけ違う意見だったので、そのまま議事録から落ちた。」を選び、「少数意見が消える」という別の含みをいったん残すため、右側の `保留状態` を `未決` にします。 | 右側の「現在の選択」が「カードを選択中」になり、まとめた2件とは別に「まだ同じまとまりだと決めていない」カードが1件残ります。 | 右側が「島を選択中」のままならEscを押してカードを選び直します。`保留状態`を`通常`に戻すか、Ctrl/Cmd+Zでも取り消せます。 |
| 6 | 画面全体を読み返します。「前の2件は、後から根拠へ戻りにくいという近さでまとめた」「残る1件は、少数意見の消失という別の含みがあるので未決にした」と説明してみます。 | 島に2カード、島の外に`未決`の1カードがあり、操作だけでなく判断理由も説明できます。 | 説明しにくい場合は、文言と配置をもう一度読み、必要なら手順3〜5をやり直します。 |
| 7 | ヘッダーの `保存` を選び、`保存しました` を確認します。 | 変更がbackendへ保存されます。 | `ドキュメントを保存できませんでした` と出た場合は再読込せず、後述の「保存できない場合」へ進みます。 |
| 8 | ブラウザを再読込し、`ドキュメントを読み込みました` を待ちます。「作業を開始」パネルが表示されている場合は右上の×（`開始パネルを閉じる`）で閉じ、島と`未決`が残っていることを確認します。`サンプルを開く`は選び直しません。 | 保存した配置と状態を再現できます。 | 読込中は操作しません。内容が消えた場合は新しい編集を加えず、[診断と障害調査](diagnostics.md)へ進みます。 |
| 9 | `共有と再現` を開き、`共有前チェック`でSafeMode、公開範囲、未レビュー情報、出力形式を読みます。この教程では書き出しや共有を実行せず、Escで閉じます。 | SafeModeがONで、未レビューの内容が共有時に制限されることを確認できます。外部共有はまだ0件です。 | 内容や共有範囲を説明できなければ実行ボタンを選ばず、[データ取り扱い](data_handling.md)を確認します。 |

## 完了チェック

- [ ] カードが3件ある。
- [ ] 「後から根拠へ戻りにくい」という近さを手がかりに、2カードを含む島を1件作った。
- [ ] 「少数意見が消える」という別の含みを残すため、1カードを島に入れず`未決`にした。
- [ ] 2件をまとめた理由と、1件をまだまとめなかった理由を説明できる。
- [ ] 保存後の再読込でも同じ状態が見える。
- [ ] `共有前チェック`でSafeModeと未レビュー情報の扱いを確認した。
- [ ] ファイルの書き出し、他者への共有、AI提案を実行していない。

ここまでできれば、kj-atlasの最初の価値である「まとまりきらない観察を、早すぎる分類で潰さず、近さと違いの両方を残しながら構造化する」ことを体験できています。次は、日常の開始・保存・終了方法を[運用手順](operations.md)で確認するか、画面全体を[現行UIカタログ](ui_catalog.md)で確認してください。

## 保存できない場合

保存に失敗しても、画面を再読込・終了するまでは内容が画面上に残っている場合があります。

1. 直ちに再読込せず、表示されたエラーを秘密情報なしで記録します。
2. `共有と再現`からDocumentV1 JSONを書き出せる場合は、共有せずローカルの安全な場所へ退避します。書き出す前に[データ取り扱い](data_handling.md)のチェック項目を確認します。
3. [診断と障害調査](diagnostics.md)の「画面は開くが保存できない」に従ってAPIとDBを確認します。
4. 解決しない場合は[サポート](../SUPPORT.md)へ進みます。ログや画像にはAPI key、token、password、実データを含めないでください。

## この教程と受け入れ確認の違い

| 文書 | 使う場面 | 完了の意味 |
| --- | --- | --- |
| この教程 | 初めて価値を体験するとき | 1本の安全な経路で、まとまりと未決を作り、その判断理由を説明したうえで保存・再確認できた。 |
| [受け入れ確認](acceptance_check.md) | 変更後や評価時に主要機能を検査するとき | マウス、キーボード、レスポンシブ、共有前確認などのsmoke testを実施した。 |
