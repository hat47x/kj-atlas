# kj-atlas デザイン設計要求（Round 6・2026-07-04）

対象: Claude Design セッションへの貼り付け用プロンプト。Round 5（パッチ居場所・段階2/3レッドライン・照合チェックリスト）は受領・採用済み。今回は整合1件の解消が主目的で、以後は実装照合レビューのループへ移行する。

---

## 貼り付け用プロンプト

Round 5 の回答を受領し、次のとおり採り込みました: パッチワークスペースは推奨どおり **(a) 選択マージタブへ統合** を採用（UX-NAV-01 に記録）。段階2/3 レッドラインは各実装 issue（⌘K=UX-CMDK-01、チートシート=UX-SHORTCUT-01、ミニマップ・一括操作バー=UX-SCALE-01）へ反映し、メニューバーは新規 issue UX-MENU-01 を起票しました。照合チェックリストは運用文書として採用し、各実装 PR に組み込みます。

今回は1点の設計矛盾の解消と、運用移行の確認です。

### 1. 【要修正】メニューバー6分類の命名矛盾

Round 5 のメニューバー分類「ファイル/編集/表示/挿入/**レビュー**/ヘルプ」には2つの問題があります:

1. **語彙分離違反**: 「レビュー」は課題3（Round 1）で確定したとおり、**データ状態（未レビュー/レビュー済み・レビューパック）に予約**した語です。表示モードの「レビュー」も「確認ビュー」へ改名した経緯があります。メニュー名に「レビュー」を使うと、せっかく分離した語義衝突が再発します。
2. **Round 1 との不整合**: Round 1（改善提案 3-1）で設計した6分類は「ファイル/編集/**カード/表示/作業/共有**」でした。挿入/レビュー/ヘルプへの変更意図が説明されていません。

Round 1 の分類（ファイル/編集/カード/表示/作業/共有）を基準に、次を検討して**確定版の6分類**を提示してください:
- 「ヘルプ」の置き場（Round 1 案には無い。? チートシート・はじめてガイド・凡例の住所として7番目に足すか、既存分類へ包含するか — CB-1 とのトレードオフ評価つきで）。
- 「挿入」が「カード」と重なるなら不要の判断を。
- 各メニューの項目割当表（既存全コマンド→分類の対応。保持系は①キャンバス/②スリムバー層に残ることを明記）。

### 2. 実装照合レビューへの運用移行（確認）

以後、新しい画面設計の依頼は原則ありません。実装が main に載るたび、実機スクリーンショット＋チェックリスト自己申告を渡しますので、✓/△/✗＋所見で回答してください。第1回は段階1（カードメタ行＋凡例・空状態）実装後です。設計判断が新たに必要になった場合（実装上の制約で設計どおりにできない等）は、その都度、選択肢とトレードオフを添えて相談します。

### 制約（毎回同じ・遵守）

採択済み視覚言語（slate構造/amber=保持系予約/4チャネル1意味/凡例既定OFF）、CB-1〜4 自己申告＋5判断軸自己評価、反スコアリング、provider=none 第一級、SafeMode 既定ON、スキーマ不変、キーボード・フォーカス契約不破壊、語彙分離（レビュー=データ状態に予約）。Pending 案件（メモ／research⇄business モード／VUI／レンズヒント）は設計対象外。リアルタイム共同編集は非目標。

### 出力形式

依頼1は確定版6分類＋項目割当表＋変更理由（Round 1 からの差分明示）。依頼2は承知の1行で十分です。

---

## 参考: プロジェクト側の受け入れ状況（背景・貼り付け不要）

- Round 5 の扱い: 新規 ADR なし。パッチ統合採用は UX-NAV-01 に記録。レッドラインは UX-CMDK-01 / UX-SHORTCUT-01 / UX-SCALE-01 に反映。UX-MENU-01 新規起票（AC-0=命名確定をブロッカー化）。照合チェックリストは `02_Architecture/design/design-qa-checklist.md` として採用。
- メニュー命名矛盾は UX-MENU-01 §1.1 に記録済み。Round 6 回答で AC-0 を解消する。
