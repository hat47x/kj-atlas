# kj-atlas デザイン設計要求（Round 7・2026-07-12）

対象: Claude Design セッションへの貼り付け用プロンプト。Round 6 で移行した実装照合ループの第2回（P31 への実装側応答＋証拠）と、新設面（外部接続レイヤー）の先行設計相談の2部構成。

---

## 貼り付け用プロンプト

第1回照合（P31）を受領し、対応しました。本ラウンドは (A) P31 指摘への実装側応答の照合と、(B) 新設面の先行設計相談です。

### A. P31 指摘への実装側応答（照合をお願いします）

1. **✗ A-3 検索一致の色チャネル** — 修正済み。ただし提示案から1点逸脱しています: 「選択系（青）または中立の専用色」のうち**青は既に選択（#2563eb）が使用中**のため、未使用の **teal を新設チャネル**として採用しました（active一致=#0d9488 3px / 通常一致=#5eead4 2px）。4チャネル1意味の原則上、「teal=検索一致」の追加が視覚言語として妥当かの確認をお願いします。回帰ガード（visual_language.guard.test.ts）にamber不使用を固定済み。
2. **△ SafeMode通知のamber面塗り** — 修正済み。中立slate地（#f8fafc/#e2e8f0）＋amberアクセント点（6px円）に変更。amber面塗りは保持系カードに限定されました。
3. **△ QA-MONKEY-11の重なり「再撮影で開閉判断」** — **開（実在の欠陥）と判定**。まず訂正: この重なりはQA-MONKEY-11（e2e文言ドリルト、記載なし）への誤帰属でした。実ブラウザで `document.elementFromPoint()` により座標検証した結果: 「配置を提案」ボタン（x:347, y:74, 91×32）の中心に、**文章化セクションの免責文言div（y:82-97、幅1408px）が恒常的に重なり**、実クリックが常に失敗（400ms待機後も座標完全一致＝タイミング起因ではない）。**QA-MONKEY-12** として証跡付きで起票済み。なお作業モードのタブ化（P21実装、UX-NAV-02起票済み）でセクション同時描画が解消されれば副次的に直る可能性が高く、修正順序はタブ化優先を予定しています。
4. **△ A-4 表示コントロール密度** — 提示方向（既定=主要のみ展開、詳細は折り畳み）を受け入れ、改善issueとして扱います（低優先）。追加設計は不要です。
5. **△ Da11y 静止画検証不可** — 補足資料: axeスモーク（8面・8/8 green、明示除外は aria-required-parent/children のみ=ADR-0052追跡）と、キーボードe2e（domain_expression_keyboard_access ほか）が常設。P25仕様表との照合はこれらの自動検査ログを一次資料としてください。

### B. 新設面の先行設計相談（ADR-0054・Proposed段階の予告）

外部接続レイヤーのADRを起票しました（段階1=read-only MCP公開 → 段階2=webhook→提案カード堆積 → 段階3=違和感の制約輸出）。人間の受理待ちのため**実装前の先行相談**です。方向だけ確認させてください:

1. **外部エージェント由来カードの視覚言語**: 段階2で、外部エージェントの観察が「提案カード（未レビュー）」として堆積します。採択済みのAI由来区別（ADR-0048 D1）を「外部エージェント由来」（agent名つき）へ拡張する必要があります。検討してほしいこと: (i) 由来表示の位置（メタ行? バッジ?）、(ii) 「堆積場」の見え方——キャンバス直置きか受け皿領域か（**通知プッシュなし・処理義務なし**の「庭」原則。Agent Inbox型の未処理キューにはしない）、(iii) 未レビューの束が増えたときの静けさの保ち方（CB-1）。
2. **「なぜ？」リンクの着地ビュー**: 外部に配信されたブリーフから根拠を辿って来訪した人が見る読み取り専用ビュー。既存のreadOnlyモード＋カードfocusで足りるか、専用の「根拠トレイル」提示（このブリーフはどのレビュー済みカードに基づくか）が要るかの判断。
3. **critique を「第4のHITLパターン」として位置づける画面語彙**: 業界の human-in-the-loop は notify/question/review（いずれも処理キューを作る）。kj-atlas の違和感は処理義務を作らない事後訂正で、この差が価値の核です。エージェント由来カードへの違和感付与が既存導線のままで成立するか、外部輸出（段階3）を踏まえた表示（「この違和感は次回のエージェントに渡ります」等の説明）が必要かの確認。

### 制約（毎回同じ・遵守)

採択済み視覚言語（slate構造/amber=保持系予約/4チャネル1意味/凡例既定OFF）、CB-1〜4 自己申告＋5判断軸自己評価、反スコアリング、provider=none 第一級、SafeMode 既定ON、スキーマ不変（agent-task.v1/agent-response.v1 は既存契約）、キーボード・フォーカス契約不破壊、語彙分離（レビュー=データ状態に予約）。Pending 案件（メモ／research⇄business モード／VUI／レンズヒント）は設計対象外。リアルタイム共同編集・通知プッシュは非目標。

### 出力形式

Aは項目ごとに ✓/△/✗＋所見（特に A-1 の teal 採否）。Bは各項目〔方向の推奨＋理由〕〔CB-1〜4 への影響〕の簡潔な回答で十分です（レッドラインは ADR Accepted 後の実装ラウンドで依頼します）。

---

## 参考: プロジェクト側の受け入れ状況（背景・貼り付け不要）

- P31 の扱い: ✗1件（A-3）と△1件（SafeMode面塗り）は PR #2563 で修正・マージ済み。△（QA-MONKEY-11重なり）は再検証の結果 **QA-MONKEY-12** として独立起票（`01_Plans/issues/done/issue-QA-MONKEY-12-work-mode-suggest-layout-button-overlap.md`、elementFromPoint による幾何学的証跡つき）。P21 と実装の乖離は **UX-NAV-02**（role=tablist・5タブ化、診断タブ新設）として起票済み。
- P31 で ✓ 評価だった A-2 開始パネル（再開導線）は、カバレッジ棚卸し（`design-request-gaps-2026-07.md`）の A-2 をクローズする扱いとする。
- B の背景: `01_Plans/adr/ADR-0054-external-connection-layer-staged-introduction.md`（Proposed）、`01_Plans/research/research-2026-07-12-trigger-ai-external-integration.md`（追補: MCP の Linux Foundation 移管・2026-07-28 仕様確定、ChatGPT/Copilot/Claude のMCPクライアント対応、TRACE の57.5%定量根拠、knowledgeplane 対照例、LangChain HITL 3パターン）。
- 実装Issue: `EXT-CONN-01/02/03`（いずれも Draft、ADR-0054 Accepted 待ちゲート）。
