# kj-atlas デザイン設計要求（Round 5・2026-07-04）

対象: Claude Design セッションへの貼り付け用プロンプト。Round 4（作業モード5タブ・領域3/5・レッドライン・a11y）は受領し、実装仕様としてプロジェクトに採り込み済み。

---

## 貼り付け用プロンプト

Round 4 の成果（作業モード5タブ完全設計・選択コンテキスト完全設計・共有前確認詳細・段階1レッドライン・a11y画面別仕様）を受領し、**実装仕様として正式採用**しました。新しい設計判断は不要と判定し（採択済み方針の適用のため）、実装 issue へ反映済みです: 共有直前サマリ（UX-SHARE-01）と a11y 横断適用（UI-QUALITY-A11Y-02）を新規起票、既存6 issue に設計参照を追記。「保護」への改名（旧・少数）と通し番号トグル既定OFFも確認しました。実装側では違和感→再提案・矛盾レビューの実装が進行中で、Round 4 設計と並走しています。

今回は3点です。

### 1. 確認: パッチ（CE3 ワークスペース）の居場所

作業モード5タブは 差分／選択マージ／AI提案／診断／文章化 ですが、現行アプリの作業モード相当には**パッチワークスペース**（差分候補の粒度別採否・プリセット・ロールバック）も含まれます。これを (a) 選択マージタブへ統合（マージ=パッチの一形態として）、(b) 独立タブとして追加（6タブ目）、(c) 当面は現状の置き場に残す、のどれにすべきか、認知負荷（CB-1〜4）と役割の重なり（選択マージとの境界）を評価して推奨してください。

### 2. 段階2・段階3のレッドライン仕様（実装引き渡し）

段階1と同じ粒度（寸法・余白・タイポ・状態遷移・出現/消滅条件）で:

- **段階2**: ⌘K コマンドパレット（候補リスト・保持系上位固定・OS別キー表記）／ショートカットチートシート（? 起動・OS切替）／メニューバー6分類＋スリムバー最終形（既存 File/Edit メニューからの移行差分を明示）／これら新設面の 390/768/960/1440px での畳み方（作業モード=モバイル下シートを含む）。
- **段階3**: ミニマップ（サイズ・位置・折りたたみ・ビュー枠操作）／一括操作バー（選択時出現・保持系左端・件数読み上げ）／島の直交描線（矩形/L字/コの字・複雑さ表示の見た目・「整える」プレビュー）／関係線エスカレーション（内部化/昇格の視覚差・昇格ラベル）。

### 3. 実装照合レビューの運用開始

今後、実装が main に載るたび実機スクリーンショットを渡します。設計意図との乖離（寸法・色チャネル・状態遷移・a11y）を指摘するレビューを依頼します。第1回は段階1（カードメタ行＋凡例・空状態）実装後を予定。レビュー観点の定型（チェックリスト）を先に提案してもらえると効率的です。

### 制約（毎回同じ・遵守）

採択済み視覚言語（slate構造/amber=保持系予約/4チャネル1意味/凡例既定OFF）、CB-1〜4 自己申告＋5判断軸自己評価、反スコアリング、provider=none 第一級、SafeMode 既定ON、スキーマ不変、キーボード・フォーカス契約不破壊。Pending 案件（メモ／research⇄business モード／VUI／レンズヒント）は設計対象外。リアルタイム共同編集は非目標。

### 出力形式

依頼1は比較表＋推奨1案。依頼2は面ごとに〔画面設計（状態別）〕〔レッドライン仕様〕〔CB自己申告〕〔5判断軸〕。依頼3はチェックリスト案。プロトタイプ更新は依頼2の代表面（⌘K・ミニマップ・一括操作バー）があれば十分です。

---

## 参考: プロジェクト側の受け入れ状況（背景・貼り付け不要）

- Round 4 の扱い: 新規 ADR なし（ADR-0047 execution-first に整合。全内容が ADR-0048 採択済み方針の適用）。
- 新規起票: `UX-SHARE-01`（共有直前サマリ・e2e）/ `UI-QUALITY-A11Y-02`（画面別 aria/フォーカス適用・e2e）。
- 設計参照追記: UX-VISUAL-01（レッドライン）/ UX-EMPTY-01（空状態条件）/ UX-VISUAL-02（「保護」改名）/ DOMAIN-TRACE-01（トグル参照実装）/ UX-NAV-01（5タブ設計=AC-2 の設計正本）。
- 実装並走: main の DOMAIN-EXPR 進行（contradictionState selector・provider=none warning・narrative grounding summary）は Round 4 §領域3 設計と整合。
