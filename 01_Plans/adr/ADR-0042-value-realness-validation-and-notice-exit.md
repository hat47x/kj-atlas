# ADR-0042: 価値実在の検証と NOTICE 脱却基準

- Status: Accepted
- Date: 2026-06-10
- Deciders: Maintainer（委譲された意思決定権限）
- Scope: `01_Plans/`, `README.md`

## Context

`README.md` は冒頭で明示している。

> 本ツールは生成AIを用いた開発中であり、現時点では人的レビュー・利用を伴っていないことをここに明示します。

機能は出揃いつつある（CE0–CE4 Done、DOMAIN-EXPR/PRODUCT-VALUE 進行、undo/redo・LLM provider 実装済み）。だが**作られた価値が実在するか——実際に人が使って「曖昧さを保留したまま思考が前に進んだ」と言えるか——は誰も検証していない**。

`ADR-0036`（価値→社会的目標）と `ADR-0037`（観測ハーネス）は観測の枠組みを定義したが、`ADR-0039` により VR4/VR5 として activation は延期されている。その結果、現段階には「価値が実在することをどう確かめ、いつ NOTICE を外すか」の**判定基準が空白**になっている。

価値最大化の観点では、これは2番目に大きい欠落である。実装をいくら積んでも、価値が届いているかの確認経路がなければ、次に何を直すべきかも分からない（改善のフィードバックループが閉じない）。

個人OSS・プレリリース段階（`ADR-0039`）では、重量級の KPI 観測やユーザー調査は過剰である。必要なのは**最小の、実行可能な自己検証（ドッグフード）と、その結果で NOTICE を段階的に外す基準**である。

## Decision

価値実在を最小コストで検証する「ドッグフード経路」と、その結果に基づく NOTICE 脱却基準を定義する。

### ドッグフード最小経路（Maintainer 自身が実施）

実ユーザー獲得を待たず、Maintainer が自分の実題材（実際に迷っている論点・未整理のメモ）で次を1回通す。

1. 空状態または自分のメモから開始し、カードを置く。
2. 少なくとも1つの保留（Hold）・違和感（Critique）・根拠不足を「失敗ではなく作業状態」として残す。
3. まとまり／関係を作り、俯瞰⇄詳細を往復する（必要なら undo で配置をやり直す）。
4. 共有前確認を通し、確定点・保留点・未レビュー情報・根拠への戻り方を含む成果物を1つ出す。
5. その過程で「思考が雑にならなかったか」「早すぎる収束を強いられなかったか」を所感として記録する。

### 検証の記録（軽量・docs-only）

- 結果は `01_Plans/` 配下の単一メモ（`dogfood-log-<date>.md`）に、所感と発見した摩擦点（friction）を箇条書きで残す。重量級のテンプレ・スコアカードは課さない。
- 摩擦点は、根幹価値を損なうもの（例：保留が削除に見える）を優先度高として issue 化する。価値を損なわない使い勝手は低優先で記録のみ。

### NOTICE 脱却の段階基準

`README.md` の NOTICE を次の段階で書き換える（削除ではなく段階表現へ）。

- **段階A（現状）**: 「人的レビュー・利用を伴っていない」。
- **段階B（ドッグフード済）**: Maintainer 自身の実題材で上記経路を1回以上完走し、根幹価値を損なう摩擦点が解消済み。NOTICE を「メンテナ自身による試用は行ったが、第三者の継続利用は未検証」に更新してよい。
- **段階C（第三者利用）**: 外部協力者または実ユーザーが1名以上、自分の題材で価値到達を報告。NOTICE を一般的な「開発中」表現に緩和してよい（`ADR-0039` の再導入トリガーと同期）。

判定は Maintainer 単独で確定してよい（`ADR-0039` の solo 段階適正化に準拠）。

### 非目標

- 行動ログ・テレメトリ・個人追跡の導入（`ADR-0036` 非監視制約を維持）。
- 定量 KPI の活性化（VR4 として延期中。本ADRは定性の自己検証に限定）。
- 公開リリースやマーケティングの計画。

## Three-Element Verification（ADR-0067 遡及適用）

| 次元 | このADRでの主張 | 他次元への制約 |
|------|----------------|---------------|
| **業務設計** | 価値実在を最小コストで検証する「ドッグフード経路」（Maintainerが実題材で1回通す）と、NOTICE 脱却の段階基準（A現状→Bドッグフード済→C第三者利用）を定義し改善フィードバックループを閉じる | 機能: 根幹価値を損なう摩擦点は優先度高で issue 化。データ: 行動ログ・テレメトリ・個人追跡は導入しない（ADR-0036 非監視制約を維持） |
| **データ設計** | 検証の記録は `01_Plans/dogfood-log-<date>.md` の単一メモにdocs-onlyで軽量に残す。NOTICE は削除ではなく段階表現（A→B→C）へ書き換える | 業務: 段階C（第三者利用報告）まで NOTICE を完全には外さない。機能: 摩擦点 issue は既存 backlog へ紐付ける |
| **機能設計** | ドッグフード経路は空状態から開始→カード配置→保留・違和感を失敗でなく作業状態として残す→俯瞰⇄詳細を往復→共有前確認を通し成果物を出す、を1回完走させる。定量KPIは活性化しない | データ: 保留が「削除に見える」摩擦は価値損傷として優先度高で記録。業務: 段階Bの条件は「根幹価値を損なう摩擦点が解消済み」であること |

## Consequences

- 期待される効果:
  - 「価値が実在するか」の確認経路ができ、改善フィードバックループが閉じる。
  - NOTICE の更新条件が明確になり、段階を恣意でなく事実で進められる。
  - 摩擦点が issue 化され、次に直すべきものが価値起点で決まる。
- 想定される副作用/制約:
  - ドッグフードは Maintainer の時間を要する（ただし1経路・定性で最小化）。
  - 自己検証はバイアスを含む → 段階C（第三者）まで NOTICE を完全には外さない設計で緩和。
- 移行時に必要な対応:
  - 実施時に `01_Plans/dogfood-log-<date>.md` を作成する。
  - 摩擦点 issue は既存 backlog（PRODUCT-VALUE / DOMAIN-EXPR）へ紐付ける。

## Traceability

- Related: `README.md`（NOTICE）, `00_Prompt/domain.md`
- Related: `01_Plans/adr/ADR-0036-value-to-social-goal-realization-roadmap.md`, `ADR-0037-value-measurement-harness-and-scorecard.md`（観測の延期枠組み）
- Related: `01_Plans/adr/ADR-0039-governance-right-sizing-personal-oss.md`（solo段階・再導入トリガー）
- Related: `01_Plans/issues/done/issue-MVP-EXIT-01-productization-readiness.md`
- Derived-from: 2026-06-10 価値最大化の不足分析（価値実在の検証経路が空白）
